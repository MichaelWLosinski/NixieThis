#include "Random.h"

Random::Random(Observer* observer, int transitionTime, int count);
: Transition(observer, transitionTime),
  m_count(count)
{
  
}

void Random::setNumber(int previous, int next)
{
 m_transitionTime  
}

void Random::update()
{
  Transition::Update();
  
  if(100 == m_percent)
  {
    m_timer.
  }
  else if(m_percent / count)
  {
    m_currentCount++;
   observer->notify(m_current, 1) 
  }
}
